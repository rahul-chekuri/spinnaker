queue_rules:
  - name: default
    merge_method: squash

pull_request_rules:
  - name: Automatically merge on CI success and review
    conditions:
      - base=main
      - "label=ready to merge"
      - "#approved-reviews-by>=1"
    actions:
      queue:
        name: default
      label:
        add: ["auto merged"]